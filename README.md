# ValueCog

This repository contains the ValueCog MVP (backend + frontend). See README_FULL.md for full instructions.

To deploy backend to Render, set the backend folder as the Root Directory and use Node environment.
